@model List<Agent>
<partial name="_Notification" />

<div class="container">
    <div class="row">
        <div class="col-6">
            <h1>List of Productss</h1>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Products" asp-action="Create" class="btn btn-primary">
                Add Products</>
            </a>
        </div>
    </div>
    <table class="table table-bordered table-striped">

        <thead>
            <tr>
                <th>
                    Products Name
                </th>
                <th>
                    Team
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model.OrderBy(u => u.Name))
            {
                <tr>
                    <td>@obj.Name</td>


                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Complete" asp-action="Edit" asp-route-id="@obj.Name" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil"></i> Edit
                            </a>
                            <a asp-controller="Products" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                <i class="bi bi-trash"></i> Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>



@* @model List<Agent>

<partial name="_Notification" />

<div class="container">
    <div class="row">
        <div class="col-6">
            <h1>List of Productss</h1>
        </div>
    </div>
    <table class="table table-bordered table-striped">

        <thead>
            <tr>
                <th>
                    Products Name
                </th>
                <th>
                    Team
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model.OrderBy(u => u.Name))
            {
                <tr>
                    <td>@obj.Name</td>


                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="CompleteCall" asp-action="Edit" asp-route-id="@obj.Name" class="btn btn-primary mx-2">
                                <i class="bi bi-pencil"></i> Edit
                            </a>
                            <a asp-controller="Products" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                <i class="bi bi-trash"></i> Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div> *@